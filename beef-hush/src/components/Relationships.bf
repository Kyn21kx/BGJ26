namespace BeefHush;

using Hush;
using System;

static class Relationships {

	// An entity with a lifetime will be created and the 
	// base entity will be stunned for however long the
	// target entity lives
	public static uint64 StunnedFor = 0;
	
}
